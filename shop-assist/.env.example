# Server-side DriftZero delivery. ShopAssist evaluates observed responses and
# sends each complete 20-interaction window to the server-resolved integration.
DRIFTZERO_API_URL=http://127.0.0.1:8000
SHOPASSIST_PUBLIC_URL=http://localhost:3000

# Server-side only. Set real values in .env.local; never commit credentials.
GROQ_API_KEY=
GEMINI_API_KEY=
